# Database (PostgreSQL / Supabase / Neon / Railway)
DATABASE_URL="postgresql://remotecut:remotecut@localhost:5432/remotecut?schema=public"

# Auth.js / Google OAuth (Single-User Owner)
AUTH_SECRET="generate-with-openssl-rand-base64-32"
AUTH_TRUST_HOST="true"
AUTH_URL="https://remote.oddpig.io.vn"
NEXTAUTH_URL="https://remote.oddpig.io.vn"

# Google Cloud OAuth 2.0 Credentials (with Gmail API enabled)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
AUTH_OWNER_EMAIL="cuongphamworks@gmail.com"

# Token Encryption Key (32-byte base64 string for encrypting OAuth refresh tokens in DB)
TOKEN_ENCRYPTION_KEY=""

# Protected Scheduled Cron Secret
CRON_SECRET=""

# Optional AI Writing Endpoint (falls back to built-in templates)
AI_API_KEY=""
AI_BASE_URL="https://api.openai.com/v1"
AI_MODEL="gpt-4o-mini"
